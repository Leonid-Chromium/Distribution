<Window x:Class="distribution.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:distribution"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="InDG" Grid.Column="0"/>
        <GridSplitter Grid.Column="1" ShowsPreview="False" Width="10"
        HorizontalAlignment="Center" VerticalAlignment="Stretch" />
        <DataGrid x:Name="OutDG" Grid.Column="2"/>
        <GridSplitter Grid.Column="3" ShowsPreview="False" Width="10"
        HorizontalAlignment="Center" VerticalAlignment="Stretch" />
        <DataGrid x:Name="EquipDG" Grid.Column="4"/>
    </Grid>
</Window>
